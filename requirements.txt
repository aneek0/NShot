# NShot не требует pip-пакетов — только стандартная библиотека Python 3.10+.
#
# Системные бинарники (установи через install.sh или apt/pacman):
#   wpa_supplicant  — движок подключения (обязателен)
#   iw              — сканирование эфира (обязателен)
#   ip (iproute2)   — управление интерфейсом (обязателен)
#   pixiewps        — офлайн-атака Pixie Dust (только для -P/--pixie-dust)
